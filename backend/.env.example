# PostgreSQL Database Connection
DATABASE_URL=postgres://username:password@hostname:5432/database_name

# Server Configuration
PORT=4000

# Example for AWS RDS:
# DATABASE_URL=postgres://pgmonitor:your_password@your-rds-endpoint.region.rds.amazonaws.com:5432/postgres

# Notes:
# - Create a read-only monitoring user for security
# - Enable pg_stat_statements extension for query tracking
# - For EC2 deployment, use private RDS endpoint
